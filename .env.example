# Copy to .env and adjust if needed
# PostgreSQL (your existing database)
DATABASE_URL=postgresql://postgres:AUktambek012@localhost:5432/Choco_factory

# If your PostgreSQL user is different, use:
# DATABASE_URL=postgresql://YOUR_USER:AUktambek012@localhost:5432/Choco_factory
